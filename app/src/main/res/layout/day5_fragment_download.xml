<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="android.view.View" />
        <variable
            name="viewModel"
            type="com.example.myapplication.model.DownloadViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <!-- 图片占位区 -->
        <ImageView
            android:id="@+id/iv_preview"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/ic_default_image"
            android:scaleType="centerCrop"
            android:visibility="@{viewModel.imageVisible ? View.VISIBLE : View.INVISIBLE}"
            android:layout_marginBottom="30dp" />

        <!-- 进度条 -->
        <ProgressBar
            android:id="@+id/progressBar"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="@{viewModel.progress}"
            android:layout_marginBottom="10dp" />

        <!-- 进度文字 -->
        <TextView
            android:id="@+id/tv_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(viewModel.progress) + `%`}"
            android:textSize="16sp"
            android:layout_marginBottom="30dp" />

        <!-- 按钮区域 -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btn_download"
                android:layout_width="100dp"
                android:layout_height="48dp"
                android:text="Download"
                android:enabled="@{!viewModel.isDownloading()}"
                android:layout_marginEnd="20dp" />

            <Button
                android:id="@+id/btn_reset"
                android:layout_width="100dp"
                android:layout_height="48dp"
                android:text="Reset" />
        </LinearLayout>
    </LinearLayout>
</layout>